---
export const prerender = true;
import { getAssetUrl } from "../utils/cdn";

const versions = [
  { id: "v1", label: "v1 · August 2026", title: "August 2026 Edition", path: "/grep/v1.pdf" },
  { id: "v0", label: "v0 · Origins", title: "Origins Edition", path: "/grep/v0.pdf" },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Read GREP - ACM-VIT</title>
    <link rel="icon" type="image/webp" href={getAssetUrl("/favicon.webp")} />
  </head>
  <body>
    <main class="grep-reader" data-grep-viewer data-mode="spread" aria-busy="true">
      <header class="reader-toolbar">
        <a class="reader-brand" href="/" aria-label="Read GREP — Back to Main Site">
          <img src={getAssetUrl("/rotate.webp")} width="20" height="20" alt="" aria-hidden="true" />
          Read <strong>GREP</strong>
        </a>
        <div class="reader-options">
          <label class="deck-field"><span>Edition</span>
            <span class="select-shell">
            <select data-grep-version-select>
              {versions.map((version, index) => (
                <option value={version.id} selected={index === 0} data-pdf-url={getAssetUrl(version.path)} data-pdf-fallback={version.path} data-version-title={version.title}>{version.label}</option>
              ))}
            </select>
            </span>
          </label>
          <details class="reading-options">
            <summary>Reading Options</summary>
          <label class="deck-field">View
            <span class="select-shell">
            <select data-grep-mode>
              <option value="spread">Two Pages</option>
              <option value="single">One Page</option>
              <option value="pdf">PDF Mode</option>
            </select>
            </span>
          </label>
          <a class="deck-button open-pdf" data-grep-pdf-link href="/grep/v1.pdf" target="_blank" rel="noopener">Open PDF ↗</a>
          </details>
        </div>
      </header>

      <section class="reader-content" aria-label="GREP pages">
        <div class="grep-stage" data-grep-stage>
          <canvas data-grep-left aria-label="Left page"></canvas>
          <canvas data-grep-right aria-label="Right page"></canvas>
          <canvas class="turn-page" data-grep-flip aria-hidden="true" hidden></canvas>
        </div>
        <iframe data-grep-pdf-frame title="GREP PDF viewer" hidden></iframe>

        <div class="reader-overlay" data-grep-start hidden>
          <div class="reader-card welcome-card">
            <div class="welcome-label">
              <span>ACM-VIT Newsletter</span>
              <img src={getAssetUrl("/rotate.webp")} width="28" height="28" alt="" aria-hidden="true" />
            </div>
            <h1>Read <span>GREP</span></h1>
            <p class="welcome-copy">Your search query for ACM-VIT updates.</p>
            <p class="welcome-edition" data-grep-edition>August 2026 Edition</p>
            <button class="deck-button primary start-reading" data-grep-start-btn type="button">
              Start Reading
              <img src={getAssetUrl("/top-arrow.webp")} width="24" height="24" alt="" aria-hidden="true" />
            </button>
          </div>
        </div>

        <div class="reader-overlay loading-overlay" data-grep-loading role="status" aria-live="polite">
          <div class="reader-card">
            <div class="loading-cassette" data-grep-loading-animation aria-hidden="true"></div>
            <p data-grep-loading-text>Loading GREP…</p>
          </div>
        </div>

        <div class="reader-overlay" data-grep-error hidden role="alert">
          <div class="reader-card">
            <h2>Unable to Load GREP</h2>
            <p>Please try again, or open the PDF directly.</p>
            <button class="deck-button primary" data-grep-retry type="button">Retry</button>
            <a class="deck-button" data-grep-error-link href="/grep/v1.pdf" target="_blank" rel="noopener">Open PDF ↗</a>
          </div>
        </div>
      </section>

      <footer class="reader-transport">
        <button class="deck-button transport-key" data-grep-prev type="button" aria-label="Previous pages" disabled>
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M15 18l-6-6 6-6" /></svg>
          <span class="sr-only">Previous pages</span>
        </button>
        <div class="page-display" aria-live="polite"><span data-grep-page>1</span><span class="page-divider"> / </span><span data-grep-total>—</span><small data-grep-end hidden>End of GREP</small></div>
        <button class="deck-button transport-key" data-grep-next type="button" aria-label="Next pages" disabled>
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M9 6l6 6-6 6" /></svg>
          <span class="sr-only">Next pages</span>
        </button>
      </footer>
      <noscript><p class="noscript">Enable JavaScript to use the reader, or <a href="/grep/v1.pdf">Read GREP v1 as a PDF</a>.</p></noscript>
    </main>
    <script>
      import "../scripts/grep-viewer";
    </script>
  </body>
</html>

<style>
  @font-face {
    font-family: "PolySans Trial";
    src: url("../assets/fonts/PolySans-trial/PolySans-trial/Standard/PolySansTrial-Slim.otf") format("opentype");
    font-weight: 300;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: "PolySans Trial";
    src: url("../assets/fonts/PolySans-trial/PolySans-trial/Standard/PolySansTrial-Neutral.otf") format("opentype");
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  :global(html), :global(body) { margin: 0; width: 100%; height: 100%; overflow: hidden; background: #0d0e0d; color: #fefcd9; font-family: "PolySans Trial", sans-serif; }
  :global(*) { box-sizing: border-box; }
  :global([hidden]) { display: none !important; }
  .grep-reader { --sidebar-width: 220px; --sidebar-gap: 16px; --reader-offset: calc(var(--sidebar-width) + 2 * var(--sidebar-gap)); padding-left: var(--reader-offset); position: relative; display: flex; flex-direction: column; height: 100vh; height: 100dvh; background: radial-gradient(circle at center, #111 0%, #0a0a0a 55%, #050505 100%); }
  /* Reserve the fixed sidebar width for pages, overlays, and transport controls. */
  .reader-toolbar { position: fixed; inset: var(--sidebar-gap) auto var(--sidebar-gap) var(--sidebar-gap); width: var(--sidebar-width); display: flex; flex-direction: column; align-items: stretch; gap: 28px; overflow-y: auto; padding: 18px; border: 1px solid rgba(254, 252, 217, .65); border-radius: 24px; background: rgba(13, 14, 13, .92); z-index: 5; }
  .reader-brand { display: inline-flex; gap: 5px; align-items: center; white-space: nowrap; text-decoration: none; color: #fefcd9; font-size: 16px; font-weight: 300; }
  .reader-brand img { margin-right: 5px; }
  .reader-brand strong { font-weight: inherit; }
  .reader-options { display: flex; flex-direction: column; align-items: stretch; gap: 20px; }
  .reading-options summary { display: flex; align-items: center; justify-content: space-between; gap: 12px; color: #fefcd9; font-size: 12px; font-weight: 300; cursor: pointer; padding: 6px 2px; list-style: none; }
  .reading-options summary::-webkit-details-marker { display: none; }
  .reading-options summary::after { content: "+"; color: #f95f4a; font-size: 20px; line-height: 1; }
  .reading-options[open] summary::after { content: "−"; }
  .reading-options summary:focus-visible { outline: 2px solid #f95f4a; outline-offset: 3px; border-radius: 4px; }
  .reading-options .deck-field { margin-top: 10px; }
  .reading-options .open-pdf { margin-top: 10px; }
  .deck-field { display: grid; gap: 8px; color: rgba(254, 252, 217, .65); font-size: 11px; font-weight: 300; }
  select, .deck-button { border: 1px solid rgba(254, 252, 217, .14); border-radius: 10px; background: #171716; color: #fefcd9; font: inherit; font-size: 12px; }
  .select-shell { display: block; position: relative; }
  .select-shell::after { content: ""; position: absolute; top: 50%; right: 18px; width: 7px; height: 7px; border-right: 1.5px solid #f95f4a; border-bottom: 1.5px solid #f95f4a; transform: translateY(-70%) rotate(45deg); pointer-events: none; }
  select { appearance: none; color-scheme: dark; width: 100%; min-height: 44px; padding: 11px 40px 11px 17px; cursor: pointer; border: 1px solid #fefcd9; border-radius: 41px; background: rgba(0, 0, 0, .35); font-size: 13px; font-weight: 300; transition: border-color .2s ease, background .2s ease; }
  select option { background: #0d0e0d; color: #fefcd9; }
  .deck-button { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 10px 20px; text-decoration: none; cursor: pointer; border-radius: 999px; border-color: #f95f4a; background: transparent; transition: background .2s ease, color .2s ease; }
  select:hover:not(:disabled) { border-color: #f95f4a; background: rgba(249, 95, 74, .08); }
  .deck-button:hover:not(:disabled) { background: #f95f4a; color: #1a1a1a; }
  .deck-button:disabled, select:disabled { opacity: .4; cursor: default; }
  :is(a, button, select):focus-visible { outline: 2px solid #f95f4a; outline-offset: 3px; }
  .open-pdf { justify-self: start; font-size: 10px; padding: 6px 12px; }
  .reader-content { position: relative; flex: 1; min-height: 0; min-width: 0; width: 100%; overflow: hidden; }
  .grep-stage { position: relative; width: 100%; height: 100%; padding: clamp(8px, 1.2vw, 18px) 80px 44px; display: flex; justify-content: center; align-items: center; }
  .grep-stage canvas { display: block; flex: 0 0 auto; }
  .grep-stage .turn-page { position: absolute; pointer-events: none; z-index: 1; will-change: transform; }
  iframe { width: 100%; height: 100%; border: 0; background: #171716; }
  .reader-overlay { position: absolute; inset: 0; display: grid; place-items: center; padding: 20px; background: radial-gradient(circle at center, rgba(12, 12, 12, .7), rgba(5, 5, 5, .92)); z-index: 3; }
  .reader-card { min-width: 0; text-align: center; max-width: 420px; padding: 26px 30px; border-radius: 18px; border: 1px solid rgba(249, 95, 74, .4); background: rgba(10, 10, 10, .7); }
  .reader-card h1 { font-size: .95rem; margin: 0 0 8px; font-weight: 400; letter-spacing: .12em; }
  .reader-card p { color: rgba(254, 252, 217, .6); font-size: .8rem; line-height: 1.6; margin: 0 0 16px; letter-spacing: .06em; }
  .reader-card h2 { font-size: 1rem; font-weight: 400; }
  .welcome-card { width: min(440px, 100%); max-width: 440px; padding: 28px; text-align: left; border: 1px solid #fefcd9; border-radius: 28px; background: #0d0e0d; }
  .welcome-label { display: flex; align-items: center; justify-content: space-between; gap: 16px; padding-bottom: 20px; margin-bottom: 24px; border-bottom: 1px solid rgba(254, 252, 217, .22); color: #fefcd9; font-size: 12px; font-weight: 300; }
  .welcome-card h1 { font-size: clamp(38px, 5vw, 52px); font-weight: 300; line-height: 1.1; letter-spacing: -.04em; margin: 0 0 16px; }
  .welcome-card h1 span { color: #f95f4a; }
  .welcome-card .welcome-copy { color: #fefcd9; font-size: 15px; font-weight: 300; letter-spacing: 0; line-height: 1.5; margin-bottom: 22px; max-width: 28ch; }
  .welcome-card .welcome-edition { display: inline-block; margin-bottom: 26px; padding: 7px 12px; border: 1px solid rgba(254, 252, 217, .35); border-radius: 41px; font-size: 11px; letter-spacing: .02em; }
  .start-reading { width: 100%; min-height: 50px; justify-content: space-between; padding: 12px 24px; border-radius: 41px; background: #f95f4a; color: #fefcd9; font-size: 16px; font-weight: 300; }
  .start-reading:hover:not(:disabled) { background: #ff7663; color: #fefcd9; }
  .start-reading img { transition: transform .2s ease; }
  .start-reading:hover:not(:disabled) img { transform: translate(2px, -2px); }
  .loading-overlay { z-index: 4; background: #0d0e0d; }
  .loading-overlay .reader-card { background: none; border: 0; }
  .loading-cassette { width: min(300px, 100%); aspect-ratio: 1; display: flex; align-items: center; justify-content: center; margin: 0 auto 26px; }
  .loading-cassette :global(svg) { max-width: 100%; max-height: 100%; }
  .reader-transport { position: absolute; inset: 0 0 0 var(--reader-offset); pointer-events: none; z-index: 2; }
  .transport-key { position: absolute; top: 50%; width: 48px; height: 48px; padding: 0; border: 1px solid rgba(249, 95, 74, .4); border-radius: 50%; background: rgba(10, 10, 10, .65); color: rgba(254, 252, 217, .75); pointer-events: auto; transform: translateY(-50%); }
  [data-grep-prev] { left: 20px; }
  [data-grep-next] { right: 20px; }
  .transport-key svg { width: 22px; height: 22px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
  .page-display { position: absolute; bottom: 16px; left: 50%; transform: translateX(-50%); text-align: center; font-size: .7rem; letter-spacing: .3em; color: rgba(254, 252, 217, .7); }
  .page-display small { display: block; font-size: 10px; letter-spacing: .05em; margin-top: 5px; }
  .page-divider { color: rgba(254, 252, 217, .4); }
  [data-mode="pdf"] .reader-transport { display: none; }
  .sr-only { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0; }
  .noscript { position: fixed; inset: 25% 10%; background: #191a17; padding: 30px; z-index: 20; }
  .noscript a { color: #f95f4a; }
  @media (max-width: 768px) {
    .grep-reader { --sidebar-width: 164px; --sidebar-gap: 8px; }
    .reader-toolbar { padding: 12px; border-radius: 18px; gap: 20px; }
    .reader-brand { font-size: 14px; }
    select { padding: 9px 28px 9px 10px; font-size: 12px; }
    .select-shell::after { right: 12px; }
    .grep-stage { padding: 8px 8px 72px; }
    .transport-key { top: auto; bottom: 12px; width: 44px; height: 44px; transform: none; }
    [data-grep-prev] { left: 8px; }
    [data-grep-next] { right: 8px; }
    .page-display { bottom: 28px; letter-spacing: .1em; }
    .page-display small { font-size: 9px; }
    .reader-overlay { padding: 10px; }
    .welcome-card { padding: 16px; border-radius: 20px; }
    .welcome-label { flex-wrap: wrap; gap: 8px; }
    .welcome-card h1 { font-size: 32px; }
    .start-reading { padding: 12px; font-size: 14px; }
  }
  @media (max-width: 480px) {
    .grep-reader { --sidebar-width: 124px; --sidebar-gap: 6px; }
    .reader-toolbar { padding: 10px 8px; }
    .reader-brand { font-size: 12px; gap: 3px; }
    .reader-brand img { width: 16px; height: 16px; margin-right: 0; }
    .reading-options summary { font-size: 11px; gap: 4px; }
    select { font-size: 11px; }
    .welcome-card { padding: 12px; }
    .welcome-card h1 { font-size: 28px; }
    .welcome-card .welcome-copy { font-size: 13px; }
    .start-reading { font-size: 12px; gap: 4px; }
    .start-reading img { width: 18px; height: 18px; }
  }
  @media (max-height: 500px) and (orientation: landscape) {
    .grep-reader { --sidebar-width: 164px; --sidebar-gap: 8px; }
    .reader-toolbar { padding: 12px; gap: 16px; border-radius: 18px; }
    .reader-brand { font-size: 14px; }
    .reader-options { gap: 12px; }
    .reading-options summary { min-height: 44px; }
  }
  @media (max-height: 650px) { .reader-overlay { align-items: start; overflow-y: auto; } }
  @media (prefers-reduced-motion: reduce) { .start-reading img { transition: none; } }
</style>
